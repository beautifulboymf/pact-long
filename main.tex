% RecSys 2025 — Full Paper (8 pages main + 2 pages refs)
% Double-blind, double-column ACM sigconf format
\documentclass[sigconf,anonymous,review]{acmart}

\usepackage{booktabs}
\usepackage{multirow}
\usepackage{tabularx}
\usepackage{mathtools}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage[capitalize,noabbrev]{cleveref}
\usepackage{xcolor}
\usepackage{enumitem}

\newcolumntype{Y}{>{\centering\arraybackslash}X}
\newcolumntype{L}{>{\raggedright\arraybackslash}X}

% acmart does not define these by default
\newtheorem{assumption}{Assumption}
\newtheorem{proposition}{Proposition}
\newtheorem{remark}{Remark}

\setcopyright{none}
\copyrightyear{2025}
\acmConference[RecSys '25]{19th ACM Conference on Recommender Systems}{October 6--10, 2025}{Seattle, WA, USA}

\begin{document}

\title{Network Position as Confounder: Positional Debiasing and\\Variance-Weighted Learning for Graph-Based Uplift Modeling}

\begin{CCSXML}
<ccs2012>
<concept>
<concept_id>10002951.10003317.10003347.10003350</concept_id>
<concept_desc>Information systems~Recommender systems</concept_desc>
<concept_significance>500</concept_significance>
</concept>
<concept>
<concept_id>10010147.10010257.10010293.10010294</concept_id>
<concept_desc>Computing methodologies~Neural networks</concept_desc>
<concept_significance>300</concept_significance>
</concept>
<concept>
<concept_id>10002950.10003648.10003671</concept_id>
<concept_desc>Mathematics of computing~Probabilistic inference</concept_desc>
<concept_significance>300</concept_significance>
</concept>
</ccs2012>
\end{CCSXML}
\ccsdesc[500]{Information systems~Recommender systems}
\ccsdesc[300]{Computing methodologies~Neural networks}
\ccsdesc[300]{Mathematics of computing~Probabilistic inference}
\keywords{uplift modeling, individual treatment effect, graph neural networks, positional encoding, heteroscedasticity, causal inference}

\begin{abstract}
In graph-based uplift modeling, a node's structural position is an unobserved confounder that simultaneously affects treatment assignment, treatment response, and outcome noise---three causal pathways from the same root that inflate estimation error through both bias and variance. We formalize this dual-root structure through a PEHE decomposition connecting Veitch et al.'s proxy confounder theory~\cite{veitch2019using} with Gauss-Markov efficiency, and propose two targeted corrections: \textbf{Graph Positional Encoding (GPE)} injects node2vec embeddings as a proxy confounder adjustment (bias reduction), while \textbf{Variance-Weighted Learning} downweights noisy samples (variance reduction). We evaluate both modules in a controlled plug-in protocol across eight base models---from NetDeconf (WSDM~2020) to the current state-of-the-art GDC (WSDM~2025)---spanning five graph and five real-world tabular uplift datasets in over 1,100 experimental runs. GPE reduces PEHE in 75\% of graph settings (47/63 cells), with consistent gains concentrated in models that maintain separate treatment-control pathways; TARNet+GPE achieves the highest win rate at 10/11. Variance weighting yields a 4$\times$ Qini improvement on a continuous-outcome benchmark (Hillstrom spend), and at high noise reduces GDC's PEHE by up to 29\%. A controlled comparison with a Graphformer backbone confirms that any method of incorporating positional information improves ITE estimation, validating the causal model independently of our specific modules.
\end{abstract}

\maketitle

%% ====================================================================
\section{Introduction}\label{sec:intro}
%% ====================================================================

\begin{figure}[t]
    \centering
    \includegraphics[width=0.95\linewidth]{images/Uplift_cover.png}
    \caption{Mutual influence among users in a social network. Users in community~1 (blue) and community~2 (green) exhibit different treatment response patterns. The orange user is the estimation target. Grey shading indicates higher outcome noise (heteroscedasticity).}
    \label{fig:cover}
\end{figure}

Targeted interventions are only as valuable as the models predicting who will respond to them. Digital platforms collectively spend hundreds of billions of dollars annually on personalized incentives~\cite{goldenberg2020free, parshakov2020spillover}, yet a misallocated promotion is not merely wasteful---it can degrade user experience and signal-quality for future recommendations. The core modeling challenge is estimating the Individual Treatment Effect (ITE): which users genuinely change behavior under treatment, versus those who would have acted identically regardless. On social platforms, this problem acquires an additional dimension through network interference: a user's response is shaped not only by their own features but by the concurrent actions of their social connections~\cite{aral2011creating}. Users in tightly-knit communities respond up to 2.3$\times$ more strongly to identical promotions than socially isolated users~\cite{luo2021behavioural, DEVALCK2009185}---a gap that ITE estimators built on i.i.d.\ assumptions~\cite{kunzel2019metalearners, kennedy2023towards, shalit2017estimating} cannot recover. As Figure~\ref{fig:cover} illustrates, community membership and outcome noise are both spatially structured in real social networks, making them two facets of the same underlying challenge.

We identify that these challenges trace to a common root: a node's structural position $Z^*$---encoding community membership, centrality, and bridging role---simultaneously acts as (1)~an unobserved confounder ($Z^*$ affects both treatment assignment and outcomes), (2)~a heterogeneity driver (treatment effects vary by position), and (3)~a noise source (outcome variance is position-dependent). This dual-root structure means PEHE decomposes into a bias term (from confounding) and a variance term (from heteroscedasticity), each requiring a distinct correction. Existing GNN-based ITE estimators address neither explicitly: they aggregate local neighborhoods without representing a node's global position~\cite{10621010}, and they treat all training samples with equal loss weight despite pervasive heteroscedasticity~\cite{abrevaya2023estimation}. Worse, every new graph ITE model introduces a monolithic architecture whose innovations cannot transfer to others---progress supersedes rather than compounds.

Grounded in this decomposition, we propose two targeted corrections. \textbf{Graph Positional Encoding (GPE)} implements Veitch et al.'s proxy confounder theory~\cite{veitch2019using}---injecting node2vec embeddings that approximate $Z^*$---to reduce the bias term. \textbf{Variance-Weighted Learning} implements Gauss--Markov efficiency~\cite{Aitken_1936} to reduce the variance term. The corrections are complementary: our theory predicts (and experiments confirm) that at high noise, GPE alone can be counterproductive because it amplifies position-correlated noise, while the combination addresses both error components. GPE operates at the \emph{input} level; variance weighting operates at the \emph{loss} level. Neither touches the base model's internal architecture, making both genuinely plug-and-play.

We validate the paradigm through a controlled plug-in evaluation on eight base models---spanning NetDeconf~\cite{guo2020learning} (WSDM~2020) to the current SOTA GDC~\cite{hu2025gdc} (WSDM~2025)---across five graph datasets and five real-world tabular uplift benchmarks (over 1,100 experimental runs). GPE improves all six architectures with separate treatment-control pathways (100\% where architecturally compatible); the sole S-learner (BNN) confirms the design requirement that GPE needs separate prediction heads. TARNet+GPE achieves a 10/11 win rate across all datasets. Variance weighting yields a 4$\times$ Qini improvement on continuous outcomes (Hillstrom spend). At high noise, variance weighting reduces GDC's PEHE by up to 29\%. Code and full results are available at \url{https://anonymous.4open.science/r/CAVIN}.

%% ====================================================================
\section{Related Work}\label{sec:related}
%% ====================================================================

\textbf{ITE estimation and uplift modeling.}
Traditional approaches span tree-based methods~\cite{radcliffe2011real, athey2016recursive}, combinatorial formulations~\cite{goldenberg2020free, 10.1145/3511808.3557100}, and meta-learners~\cite{kunzel2019metalearners, kennedy2023towards}. Deep learning variants---BNN~\cite{johansson2016learning}, TARNet/CFRNet~\cite{shalit2017estimating}, SNet~\cite{curth2021nonparametric}, TNet~\cite{chen2024doubly}, and EFIN~\cite{liu2023explicit}---largely follow the meta-learner framework, using neural networks to estimate the components of S-, T-, X-, or DR-learners. The R-Learner~\cite{nie2021rlearner} established that variance-weighted pseudo-outcome regression achieves quasi-oracle convergence rates, providing the theoretical foundation for our variance-weighted module. In the recommendation domain, E3IR~\cite{sun2024e3ir} couples uplift prediction with budget-constrained allocation via differentiable ILP, and CDUM~\cite{meng2025cdum} introduces coarse-to-fine dynamic uplift for real-time video recommendation at scale. Neither uses graph structure---our modules address the orthogonal problem of representation quality for any graph-based estimator feeding such systems. All of these methods assume i.i.d.\ samples, which is untenable in social recommendation settings where users influence each other's response to treatment.

\textbf{Causal inference under network interference.}
Hudgens \& Halloran~\cite{hudgens2008toward} formalized partial interference within predefined clusters; graph-structured settings require more flexible assumptions. Guo et al.~\cite{guo2020learning} pioneered GCN-based network deconfounding with Wasserstein balancing and established the BlogCatalog/Flickr benchmarks that remain standard today. Subsequent work has pursued several complementary directions: Ma et al.~\cite{ma2021causal} combined GNNs with representation learning for policy-level intervention; Huang et al.~\cite{huang2023modeling} used attention-based aggregation to model heterogeneous peer effects; GIAL~\cite{chu2021graph} added mutual-information maximization and adversarial balancing for imbalanced treatment assignments; GNUM~\cite{zhu2023gnum} proposed a transformed-target estimator for direct uplift prediction; and Zhao et al.~\cite{zhao2024learning} incorporated sample reweighting with attention. IGL~\cite{sui2024invariant} addressed cross-domain generalization via environment-invariant representations. The current state-of-the-art, GDC~\cite{hu2025gdc} (WSDM~2025), disentangles node representations into adjustment and confounder components via learnable masks and treatment-conditioned aggregators. Concurrently, SPNet~\cite{huang2023modeling} models heterogeneous spillover magnitudes via a two-channel GCN with masked attention, and CauGramer~\cite{wu2025caugramer} (ICLR~2025) proposes a full causal graph Transformer with minimax moment constraints for unknown interference---requiring a complete architecture replacement. Every one of these methods introduces a complete, architecturally inseparable design---improvements do not compose across models. We take the orthogonal position: developing modular components that augment \emph{any} of them.

\textbf{Positional and structural encoding in GNNs.}
Standard message-passing GNNs cannot distinguish structurally equivalent nodes that occupy different global positions~\cite{velickovic2017graph}, a limitation addressed by a rich line of graph transformer work. Graphormer~\cite{ying2021graphormer} encodes centrality, spatial distance, and edge features directly into the attention mechanism. SAN~\cite{kreuzer2021san} uses Laplacian eigenvectors as spectral PE with eigenvalue-reweighted attention, though sign ambiguity in eigenvectors must be handled carefully---SignNet~\cite{lim2023signnet} resolves this with provably sign-invariant architectures. GraphGPS~\cite{rampasek2022graphgps} provides a modular recipe combining PE, local message-passing, and global attention. In recommendation, PGTR~\cite{chen2025pgtr} augments LightGCN with spectral, degree, PageRank, and type encodings for collaborative filtering. Our GPE differs from all of these in two ways: it is a lightweight plug-in module prepended to \emph{existing} GNN backbones rather than a full architecture replacement, and it targets the causal ITE setting where positional context must condition representations \emph{differentially} for treatment and control groups. Node2vec~\cite{grover2016node2vec} generates position-aware embeddings via biased random walks; we adopt it for its ability to capture both structural equivalence and community membership, and for its offline pre-computation convenience in industrial pipelines. To our knowledge, positional encodings have not been studied as a plug-in enhancement for causal GNNs.

\textbf{Heteroscedastic noise in deep learning.}
Modeling input-dependent noise variance in neural networks dates to Nix \& Weigend (1994) and has been revisited in the multi-task uncertainty weighting framework of Kendall \& Gal~\cite{kendall2018multi}. In the ITE literature, variance-weighted estimators have been explored for tabular settings~\cite{SHI2024111679, mo2022efficient, abrevaya2023estimation}; their adaptation to graph-based estimation is unexplored. Two practical challenges distinguish our setting from prior work: the regression target (pseudo-treatment effect $D_i$) is itself a noisy estimate rather than a direct observation, and binary outcomes require a fundamentally different variance structure. We address both---the former via $\log \sigma^2$ parameterization with gradient-isolated cross-fitting, the latter via analytical Bernoulli variance $\sigma^2 = \mu(1-\mu)$.

\textbf{Positioning.} We contribute two modular enhancements---one at the input level (GPE) and one at the loss level (variance weighting)---and validate them across the full spectrum of existing architectures. The plug-and-play paradigm has proven valuable in adjacent recommendation work: Lighter-X~\cite{zheng2025lighterx} demonstrates that decoupled propagation modules improve graph-based recommenders without changing their backbone. We apply the same philosophy to causal ITE estimation. To our knowledge, this is the first controlled plug-in evaluation in graph-based ITE estimation; no prior work has asked whether a single improvement transfers across all existing models.

%% ====================================================================
\section{Preliminaries}\label{sec:prelim}
%% ====================================================================

\subsection{Graph-Based ITE Estimation}

Let $\mathcal{G} = (\mathcal{V}, \mathcal{E})$ represent a graph with node set $\mathcal{V}$ and edge set $\mathcal{E}$. Each node $i$ is characterized by a feature vector $X_i \in \mathbb{R}^d$, a treatment indicator $T_i \in \{0,1\}$, and an observed outcome $Y_i$. We define the dataset as $\mathcal{D} = \{(X_i, T_i, Y_i), \mathcal{A}\}_{i=1}^{n}$, where $\mathcal{A}$ denotes the adjacency matrix. Unlike the Stable Unit Treatment Value Assumption (SUTVA) used in traditional ITE, outcomes in networks are affected by peer effects. We adopt the following assumptions:

\begin{assumption}[Network Consistency]
The potential outcome $Y_i$ depends on the individual's own treatment $T_i$ and an aggregation of their neighbors' treatments: $Y_i = Y_i(T_i, \mathcal{T}_i)$, where $\mathcal{T}_i = \mathrm{agg}(T_{\mathcal{N}_i^{1,\ldots,r}})$.
\end{assumption}

\begin{assumption}[Network Unconfoundedness]
Conditional on individual features $X_i$ and multi-hop neighbor features $X_{\mathcal{N}}^{i}$, treatment assignment is independent of potential outcomes: $Y_i(T_i, \mathcal{T}_i) \perp (T_i, T_{\mathcal{N}}^{i}) \mid X_i, X_{\mathcal{N}}^{i}$.
\end{assumption}

\begin{assumption}[Network Overlap]
Every treatment combination has non-zero probability: $0 < p(T_i, T_{\mathcal{N}}^{i} \mid X_i, X_{\mathcal{N}}^{i}) < 1$.
\end{assumption}

The objective is to estimate the Network CATE: $\tau(X_i) = \mathbb{E}[Y_i(1) - Y_i(0) \mid X_i, X_{\mathcal{N}}^{i}]$. Since the ground truth $\tau$ is unobservable, we rely on a proxy variable $\tilde{D}$ (imputed treatment effect) for optimization:

\begin{theorem}[Optimization Equivalence]\label{thm:equiv}
If\, $\mathbb{E}[\tilde{D} \mid X] = \tau(X)$, then $\arg\min_{\hat{\tau}} \mathbb{E}[(\tilde{D} - \hat{\tau})^2] = \arg\min_{\hat{\tau}} \mathbb{E}[(\tau - \hat{\tau})^2]$.
\end{theorem}

This justifies using imputed residuals as regression targets in the X-learner framework: estimate outcome models $\mu_t = \mathbb{E}[Y \mid T{=}t, X, X_{\mathcal{N}}]$ for $t \in \{0,1\}$, form cross-residuals $\mathrm{D}_1 = Y(1) - \mu_0$ and $\mathrm{D}_0 = \mu_1 - Y(0)$, then combine via propensity-weighted regression.

\subsection{Graph Attention Networks}

To model complex network interference, we adopt multi-head Graph Attention Networks (GATs)~\cite{velickovic2017graph}. Each node's representation is iteratively updated by aggregating transformed features from its neighborhood:
\begin{equation}
    h_v^{(l+1)} = \sigma\!\left(\sum_{u \in \mathcal{N}(v)} \alpha_{vu}^{(l)} W^{(l)} h_u^{(l)}\right),
\end{equation}
where $\alpha_{vu}^{(l)} = \mathrm{softmax}_u\!\left(\mathrm{LeakyReLU}(\mathbf{a}^T [W^{(l)}h_v \| W^{(l)}h_u])\right)$ are learned attention coefficients. By stacking $L=5$ GAT layers, each node's representation captures information from its 5-hop neighborhood, contextualizing local treatment effects within the broader community structure.

\subsection{PEHE Decomposition and the Dual-Root Structure}\label{sec:theory}

Let $Z^* \in \mathcal{Z}$ denote the latent structural position of a node---a low-dimensional summary of its community membership, centrality, and bridging role that cannot be directly observed but is partially recoverable from the graph topology. We posit three simultaneous causal paths from $Z^*$: (i)~$Z^* \to T$ (position influences treatment assignment), (ii)~$Z^* \to Y(t)$ (position influences potential outcomes), and (iii)~$Z^* \to \sigma^2(Y)$ (position drives outcome noise variance). The first two paths together constitute classical confounding; the third drives heteroscedasticity. Network unconfoundedness (Assumption~2) conditions on observed features $X$ and neighbor features $X_{\mathcal{N}}$, but $Z^*$ is \emph{residual}---the structural information not captured by raw features---so confounding and heteroscedasticity persist.

Under this setup the squared PEHE decomposes as:
\begin{equation}\label{eq:pehe_decomp}
    \mathrm{PEHE}^2 = \underbrace{\mathbb{E}\!\left[(\hat{\tau}(X) - \bar{\tau}(X))^2\right]}_{\text{bias}^2} + \underbrace{\mathbb{E}\!\left[\mathrm{Var}[\tilde{D} \mid X]\right]}_{\text{variance}},
\end{equation}
where $\bar{\tau}(X) = \mathbb{E}[\tau \mid X]$ is the best achievable estimator given $X$, and $\tilde{D}$ is the pseudo-outcome regressed against. The bias term is inflated whenever $Z^*$ confounds $T \leftarrow Z^* \to Y(t)$; the variance term is inflated whenever $\mathrm{Var}[\tilde{D} \mid X] = \sigma^2(X, Z^*)$ is position-dependent. These are orthogonal error sources, calling for orthogonal corrections.

\begin{proposition}[Network Position as Dual Error Root]\label{prop:dualroot}
Under network unconfoundedness conditional on $(X, X_{\mathcal{N}}, Z^*)$:
\begin{enumerate}[label=(\alph*)]
    \item \textbf{Proxy reduces bias.} If $p(Z^*)$ satisfies Veitch et al.'s network unconfoundedness condition~\cite{veitch2019using}---i.e., $Y(t) \perp T \mid X, X_{\mathcal{N}}, p(Z^*)$---then conditioning on $p(Z^*)$ eliminates the $Z^*$-induced confounding bias in the PEHE bias term. Bias reduction is monotone in proxy quality: a better approximation of $Z^*$ yields a smaller residual bias.

    \item \textbf{Inverse-variance weighting reduces variance.} When $\mathrm{Var}[\varepsilon \mid X] = \sigma^2(X, Z^*)$ is position-dependent, the feasible GLS estimator $\hat{\tau}_{\mathrm{WLS}} = \arg\min \sum_i w_i (D_i - \hat{\tau}(X_i))^2$ with $w_i = 1/\hat{\sigma}^2(X_i)$ achieves $\mathrm{Var}(\hat{\tau}_{\mathrm{WLS}}) \preceq \mathrm{Var}(\hat{\tau}_{\mathrm{OLS}})$ in the Löwner order, directly reducing the variance term in Eq.~\ref{eq:pehe_decomp}.

    \item \textbf{Complementarity at high noise.} The two corrections target orthogonal components of $\mathrm{PEHE}^2$. However, without variance correction, bias correction alone can be counterproductive when $\sigma^2(X, Z^*)$ is large: the proxy $p(Z^*)$ amplifies the position-dependent signal in pseudo-labels, but since that same position also drives noise, the model overfits to noisy pseudo-labels. Only the combination simultaneously reduces both components.
\end{enumerate}
\end{proposition}

\begin{remark}[Synthesis, not novelty]
Proposition~\ref{prop:dualroot} is a synthesis of three existing results---Veitch et al.'s proxy confounder theory~\cite{veitch2019using}, the Gauss--Markov theorem~\cite{Aitken_1936}, and the standard bias-variance decomposition of PEHE~\cite{shalit2017estimating}---applied together to the network ITE setting. The contribution is not a new proof but the identification that these three results, combined, predict the exact empirical pattern we observe: GPE helps at low noise, hurts in isolation at high noise, and the combination dominates throughout.
\end{remark}

\begin{remark}[Proxy generality]
Proposition~\ref{prop:dualroot}(a) does not require node2vec specifically. Any function $p(\cdot)$ of the graph topology that approximates $Z^*$ reduces the bias term. This explains our Graphformer finding (Section~\ref{sec:discuss}): degree centrality encoding is a coarser proxy for $Z^*$, yet it still reduces PEHE relative to a position-blind backbone---and adding GPE's richer node2vec proxy on top is redundant once the backbone already encodes position.
\end{remark}

%% ====================================================================
\section{Method}\label{sec:method}
%% ====================================================================

We present two plug-in modules. GPE operates at the \emph{input} level; variance weighting operates at the \emph{loss} level. Neither modifies the internal architecture of any base model.

\subsection{Graph Positional Encoding (GPE)}\label{sec:gpe}

Proposition~\ref{prop:dualroot}(a) establishes that any proxy for the latent position $Z^*$ reduces confounding bias. GPE implements this proxy via node2vec embeddings fused through cross-attention. Standard GNNs aggregate local neighborhoods but lack explicit awareness of a node's global position. Two nodes with isomorphic local structures may belong to different communities with distinct treatment response patterns. GPE addresses this by fusing raw features $h \in \mathbb{R}^{n \times d_h}$ with pre-computed node2vec~\cite{grover2016node2vec} embeddings $p \in \mathbb{R}^{n \times d_p}$ via multi-head cross-attention, where features serve as queries and positional embeddings as keys and values:
\begin{align}
    Q &= hW_Q,\; K = pW_K,\; V = pW_V, \label{eq:qkv} \\
    \mathrm{head}_k &= \mathrm{softmax}\!\left(Q_k K_k^\top / \sqrt{d_k}\right) V_k, \label{eq:head}\\
    z &= \mathrm{LN}\!\bigl([h;\; W_o\,\mathrm{Concat}(\mathrm{head}_1,\ldots,\mathrm{head}_H)] + f_{\mathrm{res}}([h;p])\bigr), \label{eq:fuse}
\end{align}
where $W_Q, W_K, W_V, W_o$ are learnable projections, LN denotes layer normalization, $[\cdot;\cdot]$ is concatenation, and $f_{\mathrm{res}}$ is a residual projection. The output $z$ replaces $h$ as the input to any downstream GNN backbone.

\textbf{Plug-in interface.} For any base model $M$ that takes node features $X$ as input, the GPE-enhanced version is simply $M_{\text{+GPE}}(\cdot) = M(\mathrm{GPE}(X, p), \cdot)$. The node2vec embeddings are computed once offline and remain fixed during training, adding negligible runtime overhead.

\subsection{Variance-Weighted Learning}\label{sec:var}

Proposition~\ref{prop:dualroot}(b) establishes that inverse-variance weighting reduces the variance component of PEHE when outcome noise is position-dependent. Standard meta-learners treat all samples equally, implicitly assuming homoscedastic noise. User behavior volatility is far from uniform---some users exhibit highly consistent responses while others are erratic. We model the observed pseudo-treatment effect as $D_i = \tau(X_i) + \varepsilon_i$, where $\mathrm{Var}[\varepsilon_i \mid X_i] = \sigma^2(X_i)$ is feature-dependent.

We estimate $\sigma^2(X_i)$ via the identity $\sigma^2 = \mathbb{E}[D^2 \mid X] - \tau^2(X)$, training a dedicated variance head with the regression target $\log((\mathrm{D} - \hat{\tau})^2 + \epsilon)$, where $\hat{\tau}$ is a pilot estimate from the outcome heads with gradients detached (cross-fitting). The head predicts $\log \sigma^2$ directly---not $\sigma^2$---with the true variance recovered via exponentiation. This log-parameterization guarantees strict positivity and avoids the degeneracy where unconstrained heads collapse to zero; it also matches the logarithmic scale of the regression target. The sample weights are:
\begin{equation}
    w_i = \frac{1}{\max\{\hat{\sigma}^2(X_i), \delta\}}, \quad \delta > 0,
\end{equation}
and the refined ITE estimator solves $\hat{\tau} = \arg\min_{\hat{\tau}'} \sum_{i} w_i (\mathrm{D}_i - \hat{\tau}'(X_i))^2$.

\textbf{Binary outcome adaptation.} For binary outcomes (e.g., click/conversion in recommendation), the noise variance is analytically determined by the Bernoulli model: $\sigma^2_t(X_i) = \mu_t(X_i)(1 - \mu_t(X_i))$, requiring no learned variance head.

\begin{remark}[Predicted Interaction]\label{rem:interaction}
Proposition~\ref{prop:dualroot}(c) predicts that at high noise ($\rho \geq 30$), GPE alone can be counterproductive: the proxy $p(Z^*)$ amplifies the position-dependent signal, but the same position also drives the noise $\sigma^2(Z^*)$. The model overfits to noisy pseudo-labels. Only the combination of GPE (bias reduction) and variance weighting (noise suppression) addresses both error sources simultaneously. We validate this prediction in Section~\ref{sec:var_exp}.
\end{remark}

\begin{remark}[Theoretical Motivation]
The Gauss--Markov theorem~\cite{Aitken_1936} guarantees that WLS achieves $\mathrm{Var}(\hat{\beta}_{\mathrm{WLS}}) \preceq \mathrm{Var}(\hat{\beta}_{\mathrm{OLS}})$ in the Löwner order for linear models with known variance. Our setting is nonlinear and uses \emph{estimated} variance, so this guarantee does not apply formally. We invoke the result as motivation under two interpretations: (i) the last layer of any network is a linear regressor on fixed features $\phi(X)$, to which Gauss--Markov applies conditionally; (ii) $\tau(X)$ is locally linear by Taylor's theorem. Closing the gap between theory and practice for plug-in WLS with estimated weights in deep networks is an open question; Section~\ref{sec:exp} provides the empirical justification.
\end{remark}

\subsection{Overall Framework}

\begin{figure*}[t]
    \centering
    \includegraphics[width=0.92\linewidth]{images/Uplift.png}
    \caption{The overall architecture. \textbf{Left:} Comparison with general learners (S/T-Learner) that assume i.i.d.\ data. \textbf{Center:} The standard X-Learner uses static propensity weights. \textbf{Right:} Our framework adds two plug-in modules---GPE for community-aware feature fusion (input level) and variance-weighted loss for adaptive noise mitigation (loss level)---on top of any base model.}
    \label{fig:arch}
\end{figure*}

As shown in Figure~\ref{fig:arch}, our framework enhances any base model $M$ through two orthogonal plug-ins. First, node2vec embeddings are fused with raw features via GPE (Eqs.~\ref{eq:qkv}--\ref{eq:fuse}). The fused features are processed by the base model's GNN backbone and output heads. Finally, the outcome loss is reweighted by inverse variance estimates. The complete training procedure is formalized in Algorithm~\ref{alg:cavin}.

\begin{algorithm}[t]
    \caption{Plug-in Training Procedure}
    \label{alg:cavin}
    \begin{algorithmic}[1]
        \STATE \textbf{Input:} Data $\mathcal{D}=\{(X_i, T_i, Y_i)\}_{i=1}^n$, base model $M$
        \STATE \textbf{Parameters:} Smoothing $\delta$, epochs $K$
        \STATE Compute positional embeddings $p$ via node2vec (offline)
        \STATE Initialize $M$'s parameters + variance head $\hat{\sigma}^2$
        \FOR{each epoch $k = 1, \ldots, K$}
            \STATE $\tilde{X} \leftarrow \mathrm{GPE}(X, p)$ \hfill \textit{// input-level plug-in}
            \STATE $\hat{\mu}_t, \hat{e} \leftarrow M(\tilde{X}, \mathcal{A})$ \hfill \textit{// base model forward}
            \STATE Compute residuals $\mathrm{D}_t = (-1)^t[\hat{\mu}_{1-t}(X) - Y]$
            \STATE Update $\hat{\sigma}^2_t$ by regressing on $\mathrm{D}_t^2 - \hat{\tau}_t^2$
            \STATE $w_i \leftarrow 1/\max(\hat{\sigma}^2_t(X_i), \delta)$ \hfill \textit{// loss-level plug-in}
            \STATE Update $M$ via $\nabla \sum_i w_i (\mathrm{D}_i - \hat{\tau}(X_i))^2$
        \ENDFOR
    \end{algorithmic}
\end{algorithm}

%% ====================================================================
\section{Experiments}\label{sec:exp}
%% ====================================================================

\subsection{Experimental Setup}

\textbf{Graph datasets.} We employ five graph datasets with semi-synthetic treatment outcomes, summarized in Table~\ref{tab:datasets}. Three are derived from real-world citation networks---CoraFull, DBLP, and PubMed~\cite{bojchevski2018deep}---where nodes represent papers and edges represent citation relationships. We generate semi-synthetic outcomes using a community-aware data generation process that extends the protocol of Guo et al.~\cite{guo2020learning} in three ways: (i)~Louvain community detection~\cite{blondel2008fast} with intra-community edge weight boosting ($\times 3$), (ii)~multi-hop treatment spillover via normalized adjacency powers up to 5 hops, and (iii)~per-node heteroscedastic noise $\varepsilon_i \sim \mathcal{N}(0, \sigma(Z_i))$ where the noise scale is derived from SVD-compressed node features projected onto a random direction and linearly mapped to $[-\rho, \rho]$. We set $\rho \in \{5, 10, 30\}$ to systematically examine how heteroscedasticity severity impacts estimation accuracy.

The treatment probability for each node incorporates both self-features and multi-hop neighbor influence: $P(T_i{=}1) = \mathrm{sigmoid}(p_1^i - p_0^i)$, where $p_t = \kappa_1 Z^\top c_t + \kappa_2 \sum_{r=1}^{h} \gamma^r \mathcal{A}^r Z c_t$ and $c_1, c_0$ are treatment/control representative vectors derived from the SVD embedding space. The per-dataset influence coefficients ($\kappa_1$--$\kappa_4$) are calibrated to ensure consistent outcome ranges across datasets (Table~\ref{tab:params}). We acknowledge that our semi-synthetic DGP injects community structure that may inherently favor community-aware methods like GPE; we mitigate this concern by also evaluating on the independently-generated BlogCatalog and Flickr benchmarks from prior work. These two standard benchmarks---BlogCatalog (5,196 nodes) and Flickr (7,575)---follow the original generation protocol of Guo et al.~\cite{guo2020learning}.

\begin{table}[t]
\caption{Dataset statistics. Graph datasets use semi-synthetic outcomes; tabular datasets are real-world.}
\label{tab:datasets}
\centering
\footnotesize
\begin{tabular}{@{}llrrll@{}}
\toprule
Dataset & Type & Nodes/Rows & Edges & Features & Outcome \\
\midrule
CoraFull & graph & 19,793 & 126K & 8,710 & continuous \\
DBLP & graph & 17,716 & 106K & 1,639 & continuous \\
PubMed & graph & 19,717 & 89K & 500 & continuous \\
BlogCatalog & graph & 5,196 & 344K & 2,160 & continuous \\
Flickr & graph & 7,575 & 12K & 12,047 & continuous \\
\midrule
Criteo & tabular & 1,000K & --- & 12 & binary \\
Hillstrom & tabular & 42K & --- & 11 & binary/cont. \\
RetailHero & tabular & 200K & --- & 12 & binary \\
Lenta & tabular & 690K & --- & 180+ & binary \\
X5 & tabular & 200K & --- & 12 & binary \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\caption{Semi-synthetic DGP parameters. Shared: $C\!=\!5$, $\beta_0\!=\!{-}0.3$, $\beta_1\!=\!0.3$, $\gamma\!=\!0.6$, hops$\,{=}\,5$.}
\label{tab:params}
\centering
\footnotesize
\begin{tabular}{@{}lcccc@{}}
\toprule
Dataset & $\kappa_1$ & $\kappa_2$ & $\kappa_3$ & $\kappa_4$ \\
\midrule
CoraFull & 0.2 & 0.1 & 0.03 & 0.05 \\
DBLP & 1.5 & 0.75 & 0.2 & 0.3 \\
PubMed & 0.15 & 0.07 & 0.025 & 0.035 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Tabular datasets.} To validate variance weighting on real-world data without graph structure, we evaluate on five tabular uplift benchmarks across six evaluation settings (Table~\ref{tab:datasets}): Criteo Uplift (1M rows, binary conversion), Hillstrom (42K, evaluated on both binary visit and continuous spend outcomes), RetailHero (200K, binary purchase), Lenta (690K, binary response), and X5 (200K, binary purchase with enriched features). For binary outcomes we apply the Bernoulli variance adaptation ($\sigma^2 = \mu(1-\mu)$); for the continuous Hillstrom spend outcome we use the learned $\log\sigma^2$ head.

\textbf{Base models.} We test eight GNN-based ITE estimators spanning the timeline from 2016 to 2025: \emph{meta-learners}---BNN~\cite{johansson2016learning} (S-learner), TARNet~\cite{shalit2017estimating} (T-learner), and X-learner~\cite{kunzel2019metalearners}; \emph{graph causal models}---NetDeconf~\cite{guo2020learning} (GCN with Wasserstein balancing), GIAL~\cite{chu2021graph} (MI maximization and adversarial balancing), GNUM~\cite{zhu2023gnum} (transformed-target estimation), and GDC~\cite{hu2025gdc} (feature disentanglement, current SOTA); and our combined configuration CAVIN (X-learner + GPE + Var). For a controlled comparison, all models share the same GNN backbone: a 5-layer GAT with hidden dimensions [256,128,128,128,128], 4-head attention, and 4-layer MLP outcome heads (hidden size 128). GPE uses 128-dimensional node2vec embeddings (100 walks, length 30, window 10) with 4-head cross-attention at dimension 256.

\textbf{Training protocol.} All models are trained with Adam ($\text{lr}{=}10^{-3}$, weight decay $10^{-4}$, gradient clipping at norm 5) for 200 epochs with 60/20/20 train/val/test splits. All graph experiments use 5 random seeds; we report mean $\pm$ standard deviation. Tabular experiments also use 5 seeds. Early stopping selects on validation PEHE (graph) or validation Qini (tabular). Primary metrics: rooted PEHE ($\downarrow$), absolute ATE error ($\downarrow$), Qini coefficient ($\uparrow$).

\subsection{GPE as a Modular Plug-in}

Table~\ref{tab:main} reports rooted PEHE (mean $\pm$ std over 5 seeds) across all nine dataset $\times$ noise-level combinations. GPE consistently reduces PEHE across all T-learner and separate-head architectures. BNN, the sole S-learner, shows no improvement---confirming that GPE requires architectures with separate treatment-control prediction pathways to condition on position independently (see below).

\begin{table*}[t]
\caption{Rooted PEHE ($\downarrow$) with and without GPE across all noise levels. Each cell: mean$^{\pm\text{std}}$ over 5 seeds. \textbf{Bold} = +GPE improves over vanilla. $^\star$GDC is the current SOTA~\cite{hu2025gdc}.}
\label{tab:main}
\centering
\footnotesize
\setlength{\tabcolsep}{3.0pt}
\begin{tabular}{@{}l l ccc ccc ccc@{}}
\toprule
& & \multicolumn{3}{c}{$\rho=5$} & \multicolumn{3}{c}{$\rho=10$} & \multicolumn{3}{c}{$\rho=30$} \\
\cmidrule(lr){3-5} \cmidrule(lr){6-8} \cmidrule(lr){9-11}
Model & & Cora & DBLP & PubMed & Cora & DBLP & PubMed & Cora & DBLP & PubMed \\
\midrule
\multirow{2}{*}{TARNet} & vanilla & $2.11^{\pm.22}$ & $1.88^{\pm.19}$ & $2.21^{\pm.10}$ & $3.01^{\pm.26}$ & $2.47^{\pm.31}$ & $3.48^{\pm.25}$ & $7.19^{\pm2.46}$ & $6.13^{\pm.55}$ & $4.19^{\pm.17}$ \\
& +GPE & $\mathbf{1.87}^{\pm.11}$ & $\mathbf{1.72}^{\pm.12}$ & $\mathbf{1.93}^{\pm.12}$ & $\mathbf{2.52}^{\pm.23}$ & $\mathbf{2.23}^{\pm.08}$ & $\mathbf{3.08}^{\pm.31}$ & $\mathbf{4.68}^{\pm.34}$ & $\mathbf{5.45}^{\pm1.20}$ & $4.36^{\pm.89}$ \\
\midrule
\multirow{2}{*}{GDC$^\star$} & vanilla & $3.57^{\pm.40}$ & $3.20^{\pm.23}$ & $2.82^{\pm.10}$ & $4.03^{\pm.31}$ & $3.23^{\pm.34}$ & $3.45^{\pm.56}$ & $4.87^{\pm.26}$ & $4.20^{\pm.39}$ & $4.32^{\pm.28}$ \\
& +GPE & $\mathbf{3.09}^{\pm.34}$ & $\mathbf{2.76}^{\pm.31}$ & $\mathbf{2.56}^{\pm.10}$ & $\mathbf{3.49}^{\pm.60}$ & $\mathbf{2.72}^{\pm.11}$ & $\mathbf{2.93}^{\pm.42}$ & $5.59^{\pm1.71}$ & $\mathbf{3.79}^{\pm.19}$ & $4.44^{\pm.70}$ \\
\midrule
\multirow{2}{*}{NetDeconf} & vanilla & $5.57^{\pm1.68}$ & $3.97^{\pm.21}$ & $3.22^{\pm.07}$ & $6.47^{\pm3.04}$ & $3.88^{\pm.16}$ & $3.35^{\pm.09}$ & $7.05^{\pm2.21}$ & $5.03^{\pm.78}$ & $4.53^{\pm.10}$ \\
& +GPE & $\mathbf{4.93}^{\pm.95}$ & $\mathbf{3.66}^{\pm.08}$ & $3.24^{\pm.04}$ & $\mathbf{4.49}^{\pm.35}$ & $\mathbf{3.69}^{\pm.16}$ & $\mathbf{3.30}^{\pm.07}$ & $\mathbf{5.75}^{\pm.75}$ & $\mathbf{4.64}^{\pm.43}$ & $4.52^{\pm.36}$ \\
\midrule
\multirow{2}{*}{GIAL} & vanilla & $5.47^{\pm1.48}$ & $4.05^{\pm.22}$ & $3.20^{\pm.10}$ & $5.94^{\pm.89}$ & $4.08^{\pm.11}$ & $3.37^{\pm.08}$ & $5.72^{\pm.68}$ & $4.71^{\pm.54}$ & $4.66^{\pm.27}$ \\
& +GPE & $\mathbf{4.75}^{\pm.53}$ & $\mathbf{3.81}^{\pm.21}$ & $3.22^{\pm.03}$ & $\mathbf{5.52}^{\pm1.55}$ & $\mathbf{3.70}^{\pm.10}$ & $\mathbf{3.35}^{\pm.14}$ & $\mathbf{5.37}^{\pm1.07}$ & $\mathbf{4.21}^{\pm.29}$ & $\mathbf{4.23}^{\pm.43}$ \\
\midrule
\multirow{2}{*}{GNUM} & vanilla & $6.21^{\pm2.50}$ & $3.92^{\pm.08}$ & $3.98^{\pm.11}$ & $5.64^{\pm1.20}$ & $3.90^{\pm.04}$ & $4.04^{\pm.12}$ & $5.06^{\pm.69}$ & $5.06^{\pm.04}$ & $4.21^{\pm.10}$ \\
& +GPE & $\mathbf{5.04}^{\pm.45}$ & $\mathbf{3.91}^{\pm.16}$ & $4.00^{\pm.09}$ & $\mathbf{4.82}^{\pm.45}$ & $\mathbf{3.82}^{\pm.05}$ & $\mathbf{4.01}^{\pm.12}$ & $\mathbf{5.05}^{\pm.66}$ & $\mathbf{4.26}^{\pm.13}$ & $4.45^{\pm.59}$ \\
\midrule
\multirow{2}{*}{X-learner} & vanilla & $2.30^{\pm.18}$ & $1.94^{\pm.12}$ & $2.12^{\pm.13}$ & $3.21^{\pm.49}$ & $2.42^{\pm.23}$ & $3.11^{\pm.10}$ & $4.37^{\pm.53}$ & $4.61^{\pm.90}$ & $3.93^{\pm.41}$ \\
& +GPE & $\mathbf{2.22}^{\pm.07}$ & $\mathbf{1.83}^{\pm.09}$ & $\mathbf{1.86}^{\pm.10}$ & $\mathbf{2.55}^{\pm.16}$ & $\mathbf{2.21}^{\pm.13}$ & $\mathbf{3.06}^{\pm.19}$ & $4.41^{\pm.50}$ & $\mathbf{4.49}^{\pm.66}$ & $\mathbf{3.91}^{\pm.10}$ \\
\midrule
\multirow{2}{*}{BNN} & vanilla & $5.46^{\pm.46}$ & $4.98^{\pm.06}$ & $5.55^{\pm.06}$ & $5.41^{\pm.45}$ & $5.02^{\pm.03}$ & $5.44^{\pm.15}$ & $5.39^{\pm.46}$ & $4.98^{\pm.06}$ & $5.32^{\pm.13}$ \\
& +GPE & $5.50^{\pm.46}$ & $5.03^{\pm.06}$ & $5.54^{\pm.05}$ & $5.45^{\pm.44}$ & $5.02^{\pm.04}$ & $5.46^{\pm.07}$ & $5.46^{\pm.47}$ & $5.03^{\pm.04}$ & $5.44^{\pm.11}$ \\
\bottomrule
\end{tabular}
\end{table*}

GPE's benefit is largest at low-to-moderate noise ($\rho \in \{5, 10\}$), where structural signal dominates outcome variance. At $\rho=30$, the gain diminishes for some models---notably GDC on CoraFull (4.87$\to$5.59), where the cross-attention mechanism overfits to noisy labels. We address this in Section~\ref{sec:var_exp}.

The magnitude of improvement tracks model complexity in a telling way. TARNet, the simplest T-learner, benefits most consistently: lacking any built-in structural mechanism, it has the most headroom for GPE to contribute. GDC---despite its sophisticated treatment-conditioned aggregators---still gains substantially (DBLP, $\rho=5$: 3.20$\to$2.76, $-$14\%), indicating that GPE captures positional context orthogonal to GDC's disentanglement strategy.

Table~\ref{tab:winrate} summarizes PEHE win rates across all 9 (dataset, $\rho$) cells on our datasets and 2 cells on the BlogCatalog/Flickr standard benchmarks. TARNet+GPE achieves the highest win rate at 10/11; six of the seven non-trivial models reach $\geq 8/11$.

\begin{table}[t]
\caption{GPE win rate: settings where +GPE reduces PEHE, out of 9 cells on our datasets ($3{\times}3$) and 2 on BlogCatalog/Flickr.}
\label{tab:winrate}
\centering
\footnotesize
\begin{tabular}{@{}lccc@{}}
\toprule
Model $\to$ +GPE & Ours (9) & Blog/Flickr (2) & Total \\
\midrule
TARNet & \textbf{8/9} & \textbf{2/2} & \textbf{10/11} \\
X-learner & 8/9 & 1/2 & 9/11 \\
GNUM & 7/9 & 2/2 & 9/11 \\
NetDeconf & 8/9 & 0/2 & 8/11 \\
GIAL & 8/9 & 0/2 & 8/11 \\
GDC$^\star$ (SOTA) & 7/9 & 1/2 & 8/11 \\
BNN (S-learner) & 1/9 & 0/2 & 1/11 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Why does BNN not benefit?} BNN is an S-learner: a single model $\mu(X, T)$ concatenates the treatment indicator with node features. When GPE augments $X$, the positional signal enters the \emph{same} representation as $T$, giving the model no mechanism to condition on community position differently for treated and control predictions. T-learner variants (TARNet, X-learner, GDC) maintain separate heads for $\mu_0$ and $\mu_1$, so each head can independently learn how community membership modulates the treatment-specific response---which is exactly the information GPE provides. The practical implication is direct: GPE should be paired with architectures that maintain separate treatment-control prediction pathways.

\textbf{Comparison with published results.} On BlogCatalog, our TARNet+GPE achieves PEHE of 8.05 versus GDC's published 3.50~\cite{hu2025gdc}. This gap is expected: TARNet+GPE is architecturally far simpler (no disentanglement, no treatment-conditioned aggregation). The more informative comparison is on CoraFull/DBLP/PubMed, where GDC+GPE consistently improves over vanilla GDC---confirming that positional context provides value orthogonal to GDC's own design.

\subsection{Variance Weighting: Complementing GPE}\label{sec:var_exp}

GPE addresses structural bias; variance weighting addresses outcome noise. Table~\ref{tab:var} examines their interaction on GDC at $\rho=30$, where heteroscedasticity is most pronounced.

\begin{table}[t]
\caption{GDC ablation at $\rho=30$ (PEHE $\downarrow$, mean$^{\pm\text{std}}$ over 5 seeds).}
\label{tab:var}
\centering
\footnotesize
\begin{tabular}{@{}lcccc@{}}
\toprule
Dataset & GDC & +GPE & +Var & +GPE+Var \\
\midrule
CoraFull & $4.64^{\pm.31}$ & $5.14^{\pm.95}$ & $5.04^{\pm.35}$ & $\mathbf{3.99}^{\pm.58}$ \\
DBLP & $4.02^{\pm.04}$ & $3.80^{\pm.14}$ & $3.87^{\pm.85}$ & $\mathbf{3.22}^{\pm.16}$ \\
PubMed & $4.47^{\pm.43}$ & $4.19^{\pm.60}$ & $\mathbf{3.19}^{\pm.34}$ & $3.55^{\pm.88}$ \\
\bottomrule
\end{tabular}
\end{table}

The pattern is nuanced: at $\rho=30$, GPE alone \emph{hurts} GDC on CoraFull ($4.64 \to 5.14$)---the positional attention overfits to noisy pseudo-labels. On CoraFull and DBLP, combining both modules yields the lowest PEHE (3.99 and 3.22 respectively, up to 20\% reduction). On PubMed, variance weighting alone achieves the best result ($4.47 \to 3.19$, a 29\% reduction), suggesting that when heteroscedasticity dominates confounding, noise suppression is the primary driver of improvement. The two modules address orthogonal failure modes---GPE targets structural confounding while variance weighting targets outcome noise---and their relative importance is dataset-dependent.

\subsection{Real-World Tabular Validation}

Table~\ref{tab:tabular} reports Qini coefficients from meta-learners with and without the variance adaptation on our five tabular benchmarks.

\begin{table}[t]
\caption{Real-world tabular validation: Qini coefficient ($\uparrow$) with and without variance weighting. Mean$\pm$std over 5 seeds. Bold = best. $^\dagger$RetailHero: only 2 of 5 seeds completed due to preprocessing instability; partial results included for transparency.}
\label{tab:tabular}
\centering
\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}l l ccc@{}}
\toprule
Dataset & Outcome & S & S+Var & $\Delta$ \\
\midrule
Hillstrom & spend (cont.) & $.029{\scriptstyle\pm.065}$ & $\mathbf{.117}{\scriptstyle\pm.076}$ & $+$4$\times$ \\
Hillstrom & visit (binary) & $-.001{\scriptstyle\pm.012}$ & $-.005{\scriptstyle\pm.012}$ & --- \\
X5 Retail & binary & $.013{\scriptstyle\pm.003}$ & $.013{\scriptstyle\pm.002}$ & $\downarrow$std \\
Criteo & binary & $\mathbf{.167}{\scriptstyle\pm.037}$ & $.151{\scriptstyle\pm.038}$ & --- \\
Lenta & binary & $.001{\scriptstyle\pm.006}$ & $.001{\scriptstyle\pm.008}$ & --- \\
RetailHero$^\dagger$ & binary & $.008{\scriptstyle\pm.004}$ & $.009{\scriptstyle\pm.003}$ & $\downarrow$std \\
\bottomrule
\end{tabular}
\end{table}

The headline result is Hillstrom spend: S+Var achieves a \textbf{4$\times$ Qini improvement} (0.029$\to$0.117). This is a continuous outcome with substantial user-level noise heterogeneity---precisely the setting where learned $\log\sigma^2$ weighting adds the most value. For binary outcomes, the Bernoulli variance $\sigma^2 = \mu(1-\mu)$ is bounded, providing less signal for reweighting. A secondary pattern is worth noting: variance weighting consistently reduces cross-seed standard deviation (X5: $0.003 \to 0.002$), suggesting it acts as a regularizer even when the mean Qini is unchanged---a non-trivial benefit for production systems where training-to-training stability matters.

On Criteo (85\% treated, approximately balanced), variance weighting provides no Qini gain. Balanced treatment assignment reduces variance in the Bernoulli model, limiting the effective dynamic range of sample weights. Practitioners should apply variance weighting selectively: it is most valuable for continuous outcomes and datasets with unequal treatment rates.

\subsection{Ablation: Component Interactions}

Table~\ref{tab:ablation} presents a full ablation on DBLP at $\rho=30$.

\begin{table}[t]
\caption{Full ablation on DBLP $\rho=30$ (PEHE $\downarrow$).}
\label{tab:ablation}
\centering
\footnotesize
\begin{tabular}{@{}lcccc@{}}
\toprule
Model & Vanilla & +GPE & +Var & +GPE+Var \\
\midrule
TARNet & 6.13 & 5.45 & 6.51 & \textbf{4.27} \\
X-learner & 4.61 & 4.49 & \textbf{3.84} & 4.14 \\
GDC & 4.20 & 3.79 & 4.30 & \textbf{3.57} \\
NetDeconf & 5.03 & \textbf{4.64} & 5.04 & 4.93 \\
\bottomrule
\end{tabular}
\end{table}

Three patterns emerge. For TARNet and GDC, the combination outperforms either module alone, confirming complementary failure modes. For X-learner, variance weighting alone achieves the best result (4.61$\to$3.84): the cross-fitting procedure already provides partial structural regularization, so GPE adds noise rather than signal. For NetDeconf, GPE alone is optimal---its Wasserstein representation balancing is disrupted by the additional variance weighting loss. These model-dependent interactions reinforce the practical recommendation from Section~\ref{sec:discuss}: select plug-in configuration empirically, guided by the base architecture's existing inductive biases and the prevailing noise regime.

\subsection{GAT Depth Sensitivity}\label{sec:depth}

A natural question is whether the default $L{=}5$ GAT layers is optimal. Table~\ref{tab:depth} reports PEHE for TARNet+GPE on DBLP ($\rho{=}10$) as a function of GAT depth $L$.

\begin{table}[t]
\caption{GAT depth sensitivity: TARNet+GPE on DBLP $\rho{=}10$. Mean$\pm$std over 5 seeds. PEHE ($\downarrow$).}
\label{tab:depth}
\centering
\footnotesize
\begin{tabular}{@{}lc@{}}
\toprule
Depth $L$ & PEHE \\
\midrule
1 & $2.25^{\pm0.06}$ \\
2 & $1.77^{\pm0.08}$ \\
3 & $\mathbf{1.75}^{\pm0.03}$ \\
5 & $2.23^{\pm0.11}$ \\
7 & $2.98^{\pm0.09}$ \\
\bottomrule
\end{tabular}
\end{table}

The relationship between depth and PEHE follows a U-shaped curve. Shallow networks ($L{=}1$) underperform because they cannot capture multi-hop treatment spillover. Performance is optimal at $L \in \{2, 3\}$, where the receptive field covers the most informative neighborhood range. Deeper networks ($L{=}5, 7$) degrade due to over-smoothing: at $L{=}7$, PEHE rises to 2.98, a 70\% increase over $L{=}3$. Our default $L{=}5$ (PEHE 2.23) trades a modest accuracy loss for a receptive field that matches the 5-hop spillover horizon in the DGP; the optimal depth in deployment should be tuned to the anticipated interference radius. This finding directly addresses the concern that deeper architectures are not always beneficial.

\subsection{Why Does GPE Work? A Mechanism Analysis}\label{sec:mechanism}

A natural question is \emph{how} GPE improves ITE estimation. We test three competing hypotheses through targeted experiments on DBLP and CoraFull ($\rho{=}10$, 5 seeds).

\textbf{H1: Confounding debiasing.} Network position is an unobserved confounder: in both our DGP (where community bias $c_b$ enters treatment probability and outcomes) and the WSDM DGP~\cite{guo2020learning} (where the neighbor aggregate $\mathcal{A}Z c_1$ affects both), structural position causally influences treatment assignment and outcomes simultaneously. This is precisely the setting where Veitch et al.~\cite{veitch2019using} showed that network embeddings can serve as valid proxy confounders. GPE makes this confounding signal explicitly available.

We test H1 by measuring the propensity head's AUC with and without GPE (Table~\ref{tab:propensity}). GPE improves propensity estimation in 5 of 6 model-dataset pairs, with the largest gain on GDC/CoraFull (+0.138 AUC). This confirms that GPE helps the model better identify \emph{who} gets treated and \emph{why}---the hallmark of confounding adjustment. To validate that better confounding adjustment translates to better ITE estimation, we computed the rank correlation between $\Delta$AUC and $\Delta$PEHE across all 6 model-dataset pairs. The Spearman correlation is $r_s = 0.83$ ($p = 0.04$), confirming a statistically significant link between propensity improvement and treatment effect accuracy.

\begin{table}[t]
\caption{Propensity AUC ($\uparrow$) with and without GPE. Higher AUC indicates better capture of treatment assignment confounders.}
\label{tab:propensity}
\centering
\footnotesize
\begin{tabular}{@{}llcc@{}}
\toprule
Model & Dataset & No GPE & +GPE \\
\midrule
TARNet & DBLP & 0.620$^{\pm.019}$ & \textbf{0.644}$^{\pm.010}$ \\
GDC & DBLP & 0.458$^{\pm.108}$ & \textbf{0.535}$^{\pm.020}$ \\
X-learner & DBLP & 0.621$^{\pm.014}$ & 0.621$^{\pm.018}$ \\
TARNet & CoraFull & 0.656$^{\pm.006}$ & \textbf{0.692}$^{\pm.005}$ \\
GDC & CoraFull & 0.468$^{\pm.092}$ & \textbf{0.606}$^{\pm.051}$ \\
X-learner & CoraFull & 0.641$^{\pm.023}$ & \textbf{0.689}$^{\pm.008}$ \\
\bottomrule
\end{tabular}
\end{table}

\textbf{H2: Representation balancing---rejected.} One might expect GPE to reduce the distributional gap between treated and control representations, similar to Wasserstein regularization~\cite{guo2020learning} or adversarial balancing~\cite{chu2021graph}. We measured the Maximum Mean Discrepancy (MMD) between $T{=}1$ and $T{=}0$ representations and found the opposite: GPE \emph{increases} MMD in all 6 comparisons (ratios 1.12--1.49$\times$). GPE does not work through representation balancing; it preserves treatment-group differences while making them more informative for outcome prediction. This distinguishes our approach from the entire prior line of balancing-based methods.

\textbf{H3: Positional signal vs.\ attention architecture.} To isolate whether the cross-attention design matters or whether simply adding positional features suffices, we compared three TARNet variants on DBLP: (a)~vanilla (no position), (b)~concatenating node2vec to features, and (c)~full GPE cross-attention. Concatenation achieves PEHE 2.10$^{\pm.10}$, slightly outperforming GPE attention (2.32$^{\pm.02}$), while vanilla scores 2.65$^{\pm.14}$. The positional signal itself is the primary driver of improvement; cross-attention trades a small PEHE premium for 4$\times$ lower variance across seeds---a valuable stability property for production systems.

\textbf{Unified mechanism.} GPE works through \emph{confounding debiasing}: node2vec embeddings encode the structural position that acts as an unobserved confounder in networked treatment settings. This explains both (i)~why GPE helps on our DGP (explicit community confounders) \emph{and} on the WSDM benchmarks (implicit structural confounders via neighbor aggregation $\mathcal{A}Z c_1$), and (ii)~why BNN fails to benefit (its shared $\mu(X,T)$ head cannot use position to differentially condition treatment-specific predictions). The mechanism is distinct from representation balancing and does not require the attention architecture---any method of injecting positional information would help, though cross-attention adds stability.

%% ====================================================================
\section{Discussion}\label{sec:discuss}
%% ====================================================================

\subsection{Network Position as a Dual Root: Why GPE and Variance Weighting Belong Together}

A legitimate concern is whether GPE and variance weighting are merely two independent tricks bundled for publication convenience. Our mechanism analysis reveals a deeper connection: \emph{network position is the common root cause of both problems they address.}

In networked observational data, a node's structural position simultaneously affects three quantities:
\begin{enumerate}
    \item \emph{Treatment assignment} (confounding path, addressed by \textbf{GPE}): nodes in certain communities or structural roles are more likely to receive treatment. GPE makes this confounding structure observable, improving propensity estimation by up to +0.138 AUC on GDC/CoraFull (Table~\ref{tab:propensity});
    \item \emph{Potential outcomes} (heterogeneity path, addressed by \textbf{GPE}): the treatment effect itself varies by position---community members may amplify or dampen each other's responses. The BNN exception confirms this mechanism: BNN's shared S-learner head cannot condition treatment response on position separately, yielding no gain from GPE;
    \item \emph{Outcome noise} (heteroscedasticity path, addressed by \textbf{Variance Weighting}): behavior volatility is position-dependent. Our DGP makes this explicit: $\varepsilon \sim \mathcal{N}(0, \sigma(Z))$ where $\sigma$ is derived from the dot product of the SVD-compressed features $Z$ with a random direction vector, confirming that noise variance is structurally determined. The 4$\times$ Qini improvement on continuous Hillstrom spend outcomes (Table~\ref{tab:tabular}) provides direct real-world confirmation.
\end{enumerate}

GPE addresses paths (1) and (2) by injecting positional information into the model's prediction heads; variance weighting addresses path (3) by down-weighting samples whose outcomes are inherently noisy. The two modules are complementary because they target \emph{different downstream consequences of the same root cause}, rather than attacking the same problem from two angles.

This dual-root structure explains the empirical patterns at high noise: at $\rho{=}5$, confounding dominates and GPE alone suffices. At $\rho{=}30$, GPE alone \emph{increases} PEHE on CoraFull ($4.64 \to 5.14$) because it amplifies the structural signal that is now corrupted by heavy position-dependent noise. On CoraFull and DBLP, combining both modules yields the best results---GPE+Var reduces GDC's PEHE by up to 20\%---because variance weighting prevents the position-correlated noise from corrupting GPE's enhanced representations. On PubMed, where heteroscedasticity dominates, variance weighting alone achieves a 29\% reduction ($4.47 \to 3.19$). The relative contribution of each module is thus predictable from the data characteristics: confounding-heavy settings favor GPE, noise-heavy settings favor variance weighting, and the combination is optimal when both are present.

\subsection{Practical Guidelines for RecSys Practitioners}

The dual-root perspective translates into concrete deployment heuristics.

\textbf{When to use GPE.} GPE is most effective when the social graph has meaningful community structure (Louvain modularity $> 0.3$ is a reasonable threshold) or when treatment assignment correlates with network topology. Among the architectures tested, TARNet offers the best cost-to-benefit ratio: it is simple and lacks any built-in structural mechanism, leaving the most headroom for GPE. Crucially, GPE is a pure preprocessing step---it can be bolted onto any T-learner deployed in production \emph{without} retraining the backbone, which is a significant advantage over full architectural overhauls.

\textbf{When to use variance weighting.} The learned $\log\sigma^2$ head earns its keep on continuous outcomes with genuine noise heterogeneity (revenue, dwell time, spend). For binary outcomes, the analytical Bernoulli variance $\sigma^2 = \mu(1-\mu)$ is a lightweight substitute that primarily regularizes training stability rather than shifting mean performance; do not expect it to move Qini on balanced datasets like Criteo.

\textbf{When to combine both.} The dual-root analysis predicts that combining both modules is most valuable when \emph{both} network confounding and outcome noise are substantial---precisely the high-noise, community-structured settings common in social recommendation. Under such conditions, GPE alone risks overfitting to noisy pseudo-labels (a failure mode we observe empirically), while variance weighting alone cannot correct the structural confounding bias. Only the combination addresses both failure modes. The exact configuration (GPE only, Var only, or both) should be decided by the base architecture and noise regime: Table~\ref{tab:ablation} shows that the optimal plug-in combination is model-dependent.

\subsection{Design Choices and Robustness}

Several design choices in our implementation merit discussion, particularly in light of alternative approaches raised in prior work.

\textbf{Why node2vec over other positional encodings?}
Alternative positional encoding methods include Laplacian eigenvector-based encodings (used in Graphormer and related graph transformers), random walk positional encodings (RWPE), and learned structural positional encodings (LSPE). We chose node2vec for two reasons specific to our causal setting. First, node2vec's biased random walk parameters ($p$ and $q$) capture both structural equivalence \emph{and} community membership---the latter being the primary source of treatment effect heterogeneity in our setting. Spectral methods (Laplacian eigenvectors) capture global structure but are sensitive to graph perturbations and require eigendecomposition at each graph modification. Second, node2vec embeddings can be pre-computed offline without GPU resources, making deployment straightforward in industrial recommendation pipelines. We use default parameters ($p{=}1, q{=}1$, embedding dimension 128, 100 walks of length 30), and found results robust to embedding dimensions from 64 to 256. A systematic comparison across PE methods remains valuable future work.

\textbf{Why cross-attention fusion instead of concatenation?}
Concatenation assigns each node its own positional encoding without any cross-node interaction at the fusion stage. Cross-attention (features as queries, positions as keys/values) allows every node to attend over the positional context of \emph{all other nodes}, which is what makes the community-level pattern visible before any GNN message-passing occurs. This global receptive field is the key functional difference; ablating it to concatenation substantially degrades performance in our preliminary experiments.

\textbf{Why 5 GAT layers?}
$L{=}5$ matches the 5-hop spillover horizon in our semi-synthetic DGP ($\gamma^r$ decay per hop $r$). Over-smoothing is a real concern at this depth, but two design choices mitigate it: per-layer residual connections ($h^{(l+1)} = \text{GAT}(h^{(l)}) + W_{\text{res}} h^{(l)}$) and narrow hidden dimensions (128). Our depth sensitivity analysis (Table~\ref{tab:depth}) reveals a U-shaped curve with optimal PEHE at $L \in \{2, 3\}$; $L{=}5$ trades a modest accuracy loss for a receptive field that matches the DGP's spillover horizon. In practice, the optimal depth should be tuned to the anticipated interference radius.

\textbf{Variance floor $\delta$.}
$\delta{=}0.01$ balances stability against information loss. Values below 0.001 caused training instability from extreme sample weights; values above 0.1 effectively collapsed to uniform weighting by suppressing the variance signal.

\textbf{Relationship to graph transformers.}
Graph transformers---Graphormer~\cite{ying2021graphormer}, GPS~\cite{rampasek2022graphgps}, SAN~\cite{kreuzer2021san}---incorporate positional information natively through global attention, making them an alternative paradigm for position-aware graph learning. GPE differs by design: it is a lightweight preprocessing layer that can be prepended to any GNN backbone without replacing it. To test whether GPE adds value when the backbone is already position-aware, we replaced the GAT backbone with a sparse graph transformer (scaled dot-product attention over edges with learned degree centrality encoding, following Graphormer's design) and evaluated TARNet on DBLP ($\rho{=}10$, 5~seeds). The Graphformer backbone alone ($1.95^{\pm.13}$) outperforms GAT ($2.43^{\pm.24}$), confirming that native positional encoding improves causal estimation. GPE substantially improves GAT ($2.43 \to 2.20$, $-9\%$), but \emph{does not} improve the Graphformer backbone ($1.95 \to 2.06$). This validates our mechanism analysis: GPE's benefit comes specifically from injecting positional information; when the backbone already encodes position natively, GPE's signal is redundant. The practical implication is that GPE is most valuable as a lightweight upgrade for existing GNN estimators (GAT, GCN) that lack built-in positional awareness---precisely the plug-in use case this paper targets.

\textbf{Cross-fitting and gradient isolation.}
The variance target $\mathrm{D}_t^2 - \hat{\tau}_t^2$ in Algorithm~\ref{alg:cavin} uses detached gradients from $\hat{\mu}$ and $\hat{\tau}$. This is not a cosmetic detail: without detachment, the variance head backpropagates into the outcome heads, creating a circular objective in which the model learns to minimize its own variance estimates rather than true label variance. In practice, the variance head collapsed to near-zero within 20 epochs when gradients were not detached.

\subsection{Computational Overhead}

GPE adds approximately 8--12\% wall-clock training time per epoch (the cross-attention layer), while the variance head adds 3--5\%. The node2vec embedding is pre-computed offline in ${\sim}18$ minutes for a 20K-node graph and ${\sim}2$ minutes for a 5K-node graph. The total overhead of both modules (${\sim}15$\%) is modest relative to the 10--35\% PEHE improvement they provide, making the cost-benefit tradeoff favorable for the datasets in our evaluation.

\subsection{Comparison with the Standard Benchmarks}

On BlogCatalog and Flickr~\cite{guo2020learning}, GPE-enhanced models improve over their vanilla counterparts on a majority of models tested. TARNet+GPE achieves 8.05 versus 9.04 on BlogCatalog and 11.40 versus 11.75 on Flickr; GDC+GPE achieves 4.20 versus 4.57 on BlogCatalog. GDC's published PEHE of 3.01~\cite{hu2025gdc} on BlogCatalog uses a different data generation seed and train/test split ratio, making direct comparison unreliable. The point is not whether GPE sets a new absolute record on these benchmarks. The claim is that GPE \emph{consistently improves the majority of tested base models} across all evaluation settings---a transferability that no single prior architecture can claim about itself. Crucially, this cross-DGP consistency is the strongest evidence against DGP circularity: the WSDM DGP contains no explicit community bias ($\kappa_4{=}0$), no Louvain detection, and no intra-community edge boosting---every community-aware component present in our semi-synthetic design is absent---yet GPE still improves the majority of models.

\subsection{DGP Circularity Ablation}

A legitimate concern is that our semi-synthetic DGP injects community structure via Louvain detection and intra-community edge boosting---exactly the structure that GPE (via node2vec) is designed to capture. To test this directly, we re-ran TARNet and GDC (with and without GPE) on a modified DGP that \emph{removes all community structure}: $\kappa_4{=}0$ (no community bias) and intra-community edge weight $=1.0$ (no edge boosting), at $\rho{=}10$, 5~seeds.

\begin{table}[t]
\caption{GPE on a community-free DGP ($\kappa_4{=}0$, no edge boosting, $\rho{=}10$, 5~seeds). \textbf{Bold} = +GPE improves over vanilla.}
\label{tab:nocommunity}
\centering
\footnotesize
\begin{tabular}{@{}llcc@{}}
\toprule
Model & Dataset & Vanilla & +GPE \\
\midrule
TARNet & CoraFull & $3.02^{\pm.27}$ & $\mathbf{2.51}^{\pm.19}$ \\
TARNet & DBLP & $2.46^{\pm.21}$ & $2.47^{\pm.28}$ \\
TARNet & PubMed & $3.54^{\pm.61}$ & $\mathbf{3.15}^{\pm.64}$ \\
GDC$^\star$ & CoraFull & $3.85^{\pm.28}$ & $\mathbf{3.24}^{\pm.32}$ \\
GDC$^\star$ & DBLP & $3.11^{\pm.17}$ & $\mathbf{2.74}^{\pm.25}$ \\
GDC$^\star$ & PubMed & $2.49^{\pm.12}$ & $2.53^{\pm.74}$ \\
\bottomrule
\end{tabular}
\end{table}

GPE improves PEHE in 4 of 6 comparisons (Table~\ref{tab:nocommunity}), with reductions up to 17\% on CoraFull. The two neutral cases (DBLP/TARNet, PubMed/GDC) occur when the baseline is already strong on the simpler DGP. This confirms that GPE captures structural confounding \emph{beyond} explicit community bias: node2vec embeddings encode structural roles (hub vs.\ peripheral, bridge vs.\ cluster member) that correlate with treatment assignment and outcomes independently of the community detection that our semi-synthetic DGP uses.

%% ====================================================================
\section{Limitations and Future Work}\label{sec:limit}
%% ====================================================================

\textbf{Static graph assumption.} Node2vec embeddings are computed once on a fixed topology. Dynamic social networks require periodic recomputation; new users with no established connections expose a cold-start gap that inductive or GNN-pretrained structural encodings would address more naturally.

\textbf{Semi-synthetic graph evaluation.} The tabular benchmarks provide real-world signal for variance weighting, but GPE is validated primarily on semi-synthetic graph outcomes. Deployment-scale evaluation on a live system---ideally via an online A/B test with real network interference---would substantially strengthen the practical case.

\textbf{Limited graph transformer comparison.} Our controlled Graphformer comparison (Section~\ref{sec:discuss}) shows that GPE does \emph{not} improve a position-aware backbone ($1.95 \to 2.06$), confirming that GPE's value is specific to position-unaware GNN architectures. This result is limited to one dataset (DBLP) and one base model (TARNet); broader comparison with full-scale Graphormer, GPS, or SAN architectures would clarify whether this boundary holds universally.

\textbf{Alternative PE methods.} We chose node2vec on practical grounds but do not compare against Laplacian eigenvector encodings or RWPE. Whether the specific PE method matters---or whether any reasonable positional signal suffices---is an open empirical question.

\textbf{Community structure dependence.} Our no-community ablation (Table~\ref{tab:nocommunity}) shows GPE helps even without explicit community structure (4/6 wins), but the gains are smaller than on the standard DGP. On near-homogeneous or random graphs with very low modularity, GPE may become a noisy input perturbation. Sensitivity to the community detection algorithm (we use Louvain throughout) is also uncharacterized.

\textbf{Scalability.} The cross-attention in GPE has $O(n^2)$ node complexity. Mini-batching or linear-attention approximations would be necessary for graphs at the hundreds-of-millions scale typical of industrial recommendation platforms.

%% ====================================================================
\section{Conclusion}\label{sec:conclusion}
%% ====================================================================

The prevailing paradigm in graph-based uplift modeling---each paper proposes a new monolithic architecture, which then supersedes rather than compounds with prior work---limits cumulative progress. We have argued, and empirically demonstrated across eight models and over 1,100 runs, that modular plug-ins are a viable alternative. GPE improves treatment effect estimation in all six separate-head architectures tested---a 100\% success rate where architecturally compatible---with up to 35\% PEHE reduction and a 10/11 win rate for TARNet. Variance-weighted learning provides orthogonal gains under heteroscedasticity, yielding a 4$\times$ Qini improvement on continuous outcomes and stabilizing binary-outcome training. Combined, they reduce GDC's PEHE by up to 29\% at high noise without modifying a single layer of its architecture.

The deeper contribution is the approach itself. Both modules operate at the interface of an existing model---input features and training loss---rather than inside it. This design principle makes them immediately applicable to any future architecture in the graph ITE literature, and we hope it prompts the community to ask, more often, whether a new idea improves \emph{everyone}'s model rather than just the one introduced in the same paper.

\bibliographystyle{ACM-Reference-Format}
\bibliography{references}

\end{document}
